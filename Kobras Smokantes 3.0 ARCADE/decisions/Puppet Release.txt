political_decisions = {

   gtfo_on = { 
      alert = no
      potential = { 
         num_of_vassals_no_substates = 1
         NOT = { has_country_flag = gtfo }
      }

      allow = { ai = no } 

      effect = { set_country_flag = gtfo } 
   }

   gtfo_off = {
      alert = no
      potential = { has_country_flag = gtfo }

      allow = { ai = no } 

      effect = { clr_country_flag = gtfo }
   }

   gtfo_GCO = {
      alert = no
      potential = {
         has_country_flag = gtfo
         OR = {
            is_vassal = no
            is_substate = no
         }
      } 

      allow = {
         ai = no
      }

      effect = {
         
      }
   }
}
